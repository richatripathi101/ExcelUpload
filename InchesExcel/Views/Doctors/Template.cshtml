@model List<string>
<div class="container">
    <div class="row">
        @foreach (var imagePath in Model)
        {
            <div class="col-md-3">
                <div class="alert alert-success" role="alert">
                    <a href="/Doctors/DownloadFile/?filePath=@imagePath">
                        <img src="~/@imagePath" alt="Alternate Text" height="100px" width="205" />
                    </a>

                </div>
            </div>
        }
       <h3> 1.DUW_Template <t /> 2. Doctors_Template</h3>

    </div>



    @using (Html.BeginForm("Template", "Doctors", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        @*<div class="row">
            <div class="form-group col-md-6">
                <input type="file" class="form-control" name="myFile" />
            </div>
            <div class="form-group col-md-6">
                <input type="submit" class="filesubmit" name="Upload" />
            </div>

        </div>*@
    }

</div>